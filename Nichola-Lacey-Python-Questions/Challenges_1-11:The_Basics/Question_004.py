# Ask the user to enter two numbers. Add them together and display the answer as


fst = int(input("Enter the first number >>> "))
scd = int(input("Enter the second number >>> "))

print(f"Total of the numbers are {fst+scd}")